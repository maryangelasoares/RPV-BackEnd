<%- include('../header') %>

  <div class="container">
    <div class="row my-4">
      <div class="col-lg-12"></div>
      <div class="table-responsive">
        <table id="jobsList" class="table table-striped  text-center my-3">
          <thread>
            <tr class="table-dark">
              <th class="table-light"><a class="nav-link" href="/job/create"> <i class="fas fa-plus me-1"></i> </a>
              </th>
              <th>ID</th>
              <th>Nome</th>
              <th>Salário</th>
              <th> </th>
            </tr>
            <tbody>
              <% if ( jobs !='' ) { jobs.forEach((row, index)=> { %>
                <tr class="align-middle">
                  <td>

                  </td>
                  <td>
                    <%= row.id %>
                  </td>
                  <td>
                    <%= row.nome %>
                  </td>
                  <td> R$
                    <%= row.salario.toFixed(2) %>
                  </td>
                  <td>
                    <a href="job/update/<%= row.id %>" class="text-dark">
                      <i class="fas fa-edit fa-lg mx-1"></i></a>
                    <a href="job/delete/<%= row.id %>" class="text-dark">
                      <i class="fas fa-trash-alt fa-lg mx-1"></i></a>
                  </td>
                </tr>
                <% }) } %>
            </tbody>
          </thread>
        </table>
        <% if ( jobs=='' ) { %>
          <h1 class="text-center text-secondary mt-5">
            Nenhuma vaga de emprego encontrada! :(
          </h1>
          <% } %>
      </div>
    </div>
  </div>

<%- include('../footer') %>